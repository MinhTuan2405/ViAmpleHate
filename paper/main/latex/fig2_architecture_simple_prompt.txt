=====================================================================
HÌNH 2 (BẢN ĐƠN GIẢN) — Kiến trúc tổng quát ViAmpleHate
Mục tiêu: truyền tải Ý TƯỞNG dự án, không đào sâu công thức.
Dùng cho công cụ vẽ diagram (draw.io / Excalidraw / PowerPoint / Mermaid)
hoặc đưa cho AI sinh hình.
=====================================================================

PROMPT:

"Draw a simple, clean left-to-right block diagram for a Vietnamese
hate-speech detection model called 'ViAmpleHate'. Flat boxes, rounded
corners, thin arrows, minimal text, 2 colors only (gray = standard parts,
one accent color = the new ideas). Keep it high-level — no formulas.

Flow (left to right):

[ Vietnamese comment ]
        |
        v
[ Preprocessing ]   (normalize + word-segment, small caption only)
        |
        +--------------------------+
        |                          |
        v                          v
[ PhoBERT encoder ]        [ Target & Attack detection ]   <-- accent
   (sentence meaning)        - Vietnamese NER + target cues  --> who is targeted
                             - attack cues                   --> hostile words
        |                          |
        |                          v
        |                  [ Relation attention ]            <-- accent
        |                   (3 views: target / context / attack)
        |                          |
        +-----------+--------------+
                    v
            [ Adaptive gate ]                                <-- accent
         (decides how much the target/attack
          evidence affects the decision, per comment)
                    |
                    v
            [ Classifier ] --> [ NON-HATE / HATE ]

Notes:
- Highlight in the accent color the three NEW ideas: (1) Vietnamese
  target + attack cue detection, (2) relation attention with 3 views,
  (3) adaptive gate. These are the project's core contribution.
- Keep PhoBERT, preprocessing, and classifier in neutral gray.
- One short tagline under the title is fine, e.g.:
  'Target-aware hate detection adapted to Vietnamese.'
- Aim for something readable at a glance, suitable for a slide or a
  two-column paper figure."

=====================================================================
NOTE (LaTeX): khi có ảnh, thay
  \includegraphics[width=0.9\linewidth]{example-image}
bằng
  \includegraphics[width=0.9\linewidth]{fig2_architecture}
và bỏ '[DRAW NEW]' trong caption.
=====================================================================
